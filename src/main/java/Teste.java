import java.util.List;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.CriteriaQuery;
import javax.persistence.criteria.Join;
import javax.persistence.criteria.Root;

import br.com.swaggsige.model.domain.Cliente;
import br.com.swaggsige.model.domain.EnderecoCliente;


public class Teste {

	public static void main(String[] args) {
		/*List<EnderecoCliente> listaEnd = new ArrayList<EnderecoCliente>();



		
		
		
		Usuario usuario = new Usuario();
		usuario.setLogin("teste");
		usuario.setSenha("junu");
		usuario.setPermissao(PermissaoAcesso.CLIENTE);
		
		Cliente cliente = new Cliente();
		cliente.setNome("Sergio Junior");
		cliente.setUsuario(usuario);
		
		//cliente.setEnderecoCliente(listaEnd);
		
		EnderecoCliente endCliente = new EnderecoCliente();
		endCliente.setBairro("Planalto");
		endCliente.setCep("13485084");
		endCliente.setCidade("Limeira-SP");
		endCliente.setComplemento("Casa");
		endCliente.setCliente(cliente);
		listaEnd.add(endCliente);
	    try {
	    	new BaseRepository<Usuario>(Usuario.class).saveOrUpdate(usuario);
	    	new BaseRepository<Cliente>(Cliente.class).saveOrUpdate(cliente);
			new BaseRepository<EnderecoCliente>(EnderecoCliente.class).saveOrUpdate(endCliente);
		} catch (PersistenceException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}*/
	   
	 
		
		
		

	}

}
